apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-ci-pipeline
  namespace: $(APP_NAME)
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: default
  resources:
    requests:
      storage: 30Gi
